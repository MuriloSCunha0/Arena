/**
 * Utilitário para monitorar o desempenho da aplicação e detectar bloqueios
 * Versão simplificada para evitar impactos na performance
 */

const performanceMonitor = {
  startMonitoring: () => {},
  stopMonitoring: () => {}
};

export default performanceMonitor;
